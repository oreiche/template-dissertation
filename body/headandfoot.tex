% Set header and footer
% example: \(i|c|o)(head|foot)[first page of chapter]{other pages}
% i(inner), c(center), o(outer)
\ohead           {\textsl{\headmark}}
\ofoot[\pagemark]{\pagemark}

% Head and foot strokes
\setheadsepline{0.4pt}
%\setfootsepline{0.4pt}
